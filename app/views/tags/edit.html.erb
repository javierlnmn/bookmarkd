<% content_for :hide_modal_cancel do %>true<% end %>
<% content_for :modal_size do %>max-w-lg<% end %>

<%= render "form", tag: @tag %>

<% content_for :modal_footer do %>
  <%= link_to "Go back",
    tags_path,
    data: { turbo_frame: "modal" },
    class: "cursor-pointer px-4 py-2 text-zinc-400 hover:text-zinc-100 hover:bg-zinc-800 rounded-xl text-sm font-semibold transition-colors" %>
<% end %>
