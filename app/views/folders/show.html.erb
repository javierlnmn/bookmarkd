<div class="max-w-6xl w-11/12 mx-auto">
  <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-8">
    <div>
      <h1 class="text-3xl font-bold text-white mb-1"><%= @folder.name %></h1>
      <p class="text-zinc-500 text-sm font-medium">Organize and access your favorite links inside the folder</p>
    </div>

    <%= @folder.get_path %>

    <div class="flex gap-2 items-center">
      <div data-controller="dropdown" class="relative">
        <button data-action="dropdown#toggle" data-dropdown-target="button" class="inline-flex items-center justify-center gap-2 px-5 py-2.5 bg-zinc-100 hover:bg-white text-zinc-950 font-bold rounded-xl transition-all active:scale-95 shadow-lg shadow-white/5 cursor-pointer">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
          Add New
        </button>

        <div data-dropdown-target="list" class="hidden absolute top-[calc(100%+8px)] right-0 w-48 bg-zinc-900 border border-zinc-800 rounded-2xl p-2 shadow-2xl z-50 animate-in fade-in zoom-in duration-200">
          <%= link_to new_folder_bookmark_path(@folder), 
              data: { turbo_frame: 'modal', action: "click->dropdown#hide" },
              class: "flex items-center gap-3 px-4 py-3 text-zinc-300 hover:text-white hover:bg-zinc-800 rounded-xl transition-colors text-sm font-semibold group" do %>
            <svg class="w-4 h-4 text-zinc-500 group-hover:text-orange-400 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"/></svg>
            New Bookmark
          <% end %>

          <%= link_to new_folder_folder_path(@folder), 
              data: { turbo_frame: 'modal', action: "click->dropdown#hide" },
              class: "flex items-center gap-3 px-4 py-3 text-zinc-300 hover:text-white hover:bg-zinc-800 rounded-xl transition-colors text-sm font-semibold group" do %>
            <svg class="w-4 h-4 text-zinc-500 group-hover:text-blue-400 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"/></svg>
            New Folder
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <%= render "folders/content", folders: @folders, bookmarks: @bookmarks %>
</div>

</div>
