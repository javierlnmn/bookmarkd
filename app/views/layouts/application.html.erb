<!DOCTYPE html>
<html>
  <%= render "layouts/head" %>
  <body class="bg-zinc-950 text-zinc-100 line-seed min-h-screen selection:bg-orange-500/30">
    <header class="sticky top-0 z-40 bg-zinc-950/80 backdrop-blur-md border-b border-zinc-800">
      <div class="max-w-6xl w-11/12 mx-auto h-16 flex items-center justify-between">
        <div class="flex items-center gap-2">
          <%= image_tag "/icon.svg", class: "w-8 h-8 drop-shadow-lg drop-shadow-orange-500/20" %>
          <span class="font-bold text-xl tracking-tight bg-clip-text text-transparent bg-gradient-to-r from-white to-zinc-400">Bookmarkd</span>
        </div>
        
        <div class="relative flex items-center gap-6" data-controller="dropdown">
          <nav class="hidden md:flex items-center gap-6">
            <% if authenticated? %>
              <a href="/" class="text-sm font-medium text-zinc-400 hover:text-white transition-colors">Home</a>
              <%= link_to tags_path, data: { turbo_frame: 'modal' }, class: "text-sm font-medium text-zinc-400 hover:text-white transition-colors" do %>
                Tags
              <% end %>
              <%= button_to "Log out", session_path, method: :delete, class: "text-sm font-medium text-zinc-400 hover:text-white transition-colors" %>
              <p class="text-sm text-orange-400"><%= Current.user.email_address %></p>
            <% else %>
              <%= link_to new_session_path, class: 'text-sm font-medium text-zinc-400 hover:text-white transition-colors' do %>Log in<% end %>
            <% end %>
          </nav>

          <button type="button" class="md:hidden p-2.5 text-zinc-400 hover:text-white hover:bg-zinc-800 rounded-xl transition-colors" data-action="click->dropdown#toggle" aria-label="Open menu">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
          </button>

          <div data-dropdown-target="list" class="hidden md:hidden flex flex-col absolute top-full right-0 mt-2 w-56 bg-zinc-900 border border-zinc-800 rounded-xl shadow-xl z-50 py-2">
            <% if authenticated? %>
              <%= link_to "Home", "/", class: "px-4 py-2.5 text-sm font-medium text-zinc-400 hover:text-white hover:bg-zinc-800 transition-colors", data: { action: "click->dropdown#hide" } %>
              <div class="px-4 py-2.5 text-sm text-orange-400 border-t border-zinc-800 mt-1 pt-2"><%= Current.user.email_address %></div>
              <%= button_to "Log out", session_path, method: :delete, class: "w-full text-left px-4 py-2.5 text-sm font-medium text-zinc-400 hover:text-white hover:bg-zinc-800 transition-colors", data: { action: "click->dropdown#hide" } %>
            <% else %>
              <%= link_to "Log in", new_session_path, class: "px-4 py-2.5 text-sm font-medium text-zinc-400 hover:text-white hover:bg-zinc-800 transition-colors", data: { action: "click->dropdown#hide" } %>
            <% end %>
          </div>
        </div>
      </div>
    </header>

    <main class="py-8">
      <%= yield %>
    </main>

    <%= turbo_frame_tag 'modal' %>
    
    <%= render "layouts/confirm_dialog" %>
  </body>
</html>
