<dialog id="confirm" class="bg-zinc-900 border border-zinc-800 p-6 rounded-2xl shadow-2xl backdrop:backdrop-blur-md backdrop:bg-black/80 max-w-sm w-11/12 fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 m-0 open:flex flex-col">
  <div class="flex flex-col gap-4">
    <h3 class="text-xl font-bold text-white">Are you sure?</h3>
    <p class="text-zinc-400 text-sm leading-relaxed" data-turbo-confirm-message-target>
      This action cannot be undone.
    </p>
    <div class="flex justify-end gap-3 mt-2">
      <button class="confirm-cancel px-4 py-2 text-zinc-500 hover:text-zinc-300 text-sm font-semibold transition-colors cursor-pointer">
        Cancel
      </button>
      <button id="confirm-accept" class="px-5 py-2 bg-red-500 hover:bg-red-400 text-white text-sm font-bold rounded-xl transition-all active:scale-95 cursor-pointer">
        Confirm
      </button>
    </div>
  </div>
</dialog>
